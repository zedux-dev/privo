<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: black;">
  <div class="container-fluid" style="padding: 0 40px;">
    <a class="navbar-brand me-auto" style="font-family: 'Alegreya', serif; font-size: 35px; padding: 0;" href="/">Privo</a>
      
    <div class="d-flex">
      <div class="navbar-nav" style="display: flex; justify-content:center; align-items: center;">
        <a class="nav-link" href="/">Home</a>
        <a class="nav-link active" aria-current="page" href="">Accedi</a>
        <a class="nav-link" href="/signup">Registrati</a>
      </div>
    </div>
  </div>
</nav>

<div id="login" v-cloak>
  <div class="container-fluid pt-5 pb-5">
    <h1 class="text-center" style="font-family: San Francisco;">Accedi con il tuo account</h1>
    <div style="max-width: 450px;" class="mx-auto">
      <hr style="opacity: 0.15; margin-bottom: 40px;">
      <ajax-form action="login" :syncing.sync="syncing" :cloud-error.sync="cloudError" :form-data="formData" :form-rules="formRules" :form-errors.sync="formErrors" @submitted="submittedForm()">

        <div class="form-group" style="margin-bottom: 10px;">
          <input type="email" class="form-control" placeholder="Email address" :class="[formErrors.emailAddress ? 'is-invalid' : '']" v-model.trim="formData.emailAddress" autocomplete="email" focus-first>
          <div class="invalid-feedback" v-if="formErrors.emailAddress">Per favore inserisci un indirizzo email valido.</div>
        </div>

        <div class="form-group" style="margin-bottom: 10px;">
          <input type="password" class="form-control" placeholder="Password" :class="[formErrors.password ? 'is-invalid' : '']" v-model.trim="formData.password" autocomplete="current-password">
          <div class="invalid-feedback" v-if="formErrors.password">Per favore inserisci la password.</div>
        </div>

        <div class="form-group form-check">
          <input class="form-check-input" type="checkbox" id="remember" name="rememberMe" v-model="formData.rememberMe"/>
          <label class="form-check-label" for="remember">Ricordami</label>
        </div>

        <cloud-error style="margin-top: 10px;" v-if="cloudError==='badCombo'">Le credenziali usate non sono collegate a nessun account. Ciò vuol dire che la mail e/o la password sono errate.</cloud-error>
        <cloud-error v-else-if="cloudError"></cloud-error>

        <div class="form-group">
          <ajax-button :syncing="syncing" class="btn-dark btn-lg btn-block" style="margin-top: 30px; width: 100%;">Accedi</ajax-button>
        </div>

      </ajax-form>
    </div>
  </div>
</div>
<%- /* Expose locals as `window.SAILS_LOCALS` :: */ exposeLocalsToBrowser() %>
